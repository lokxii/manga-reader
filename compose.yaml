services:
  backend:
    build: backend
    ports:
      - 5000:5000
    volumes:
      - ./backend:/app
  frontend:
    build:
      context: frontend
      target: final
    ports:
      - 8000:8000
